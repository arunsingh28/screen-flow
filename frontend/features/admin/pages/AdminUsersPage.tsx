import AdminDashboard from './AdminDashboard';

export default function AdminUsersPage() {
    // For now, just use the same dashboard content
    // This shows the users table which is what we want
    return <AdminDashboard />;
}
